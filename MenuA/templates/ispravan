{% load static %}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Coolinarika</title>
    <link rel="stylesheet" href="{% static 'css/sidebar.css' %}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <img src="{% static 'img/logo.png' %}" alt="logo">
</head>
<body>
<div class="sidenav">
    <div class="side_logo">
        <a href="/">
            <img src="{% static 'img/logo.png' %}" alt="logo">
        </a>
    </div>
    <div class="main_menu">
        <div class="main_menu_a">
			{% if user.is_authenticated %}
					{% for menu in menues %}
						{% if menu.menuid == "main" %}
						    <li>
						        <a href="{{ menu.url }}">
						            <i class="{{menu.icon}}"></i>{{menu.title}}
						        </a>
						    </li>
						{% endif %}
					{% endfor %}
				<div class="main_menu_user">
					{% for menu in menues %}
						{% if menu.menuid == "usermenu" %}
						    <li>
						        <a href="{{ menu.url }}">
						            <i class="{{menu.icon}}"></i>{{menu.title}}
						        </a>
						    </li>
						{% endif %}
					{% endfor %}
				</div>
			{% else %}
					{% for menu in menues %}
						{% if menu.menuid == "main" %}
						    <li>
						        <a href="{{ menu.url }}">
						            <i class="{{menu.icon}}"></i>{{menu.title}}
						        </a>
						    </li>
						{% endif %}
					{% endfor %}
			{% endif %}
        </div>
        <div class="main_menu_b">
			{% for menu in menues %}
				{% if menu.menuid == "bottom_menu" %}
					<li>
						<a href="{{ menu.url }}">
						    <i class="{{menu.icon}}"></i>{{menu.title}}
						</a>
					</li>
				{% endif %}
			{% endfor %}
        </div>
    </div>
    <div class="profil-btn">
        <a href="/profil">
            <i class="fa fa-user-circle-o"></i> Moj profil
        </a>
    </div>
</div>
</body>
</html>
